# Leave unset for local dev — the Vite dev server proxies /api to localhost:3001 (see vite.config.ts).
# Set this only for production builds (e.g. Netlify) to the deployed API's URL, no trailing slash.
# VITE_API_URL=https://your-api.onrender.com
